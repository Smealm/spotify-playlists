name: Sync Dubstep Don Playlist

on:
  workflow_dispatch:
  schedule:
    - cron: "0 16 * * *"

jobs:
  sync-dubstep-don:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Sync Dubstep Don archive
        env:
          SPOTIFY_CLIENT_ID: ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_CLIENT_SECRET: ${{ secrets.SPOTIFY_CLIENT_SECRET }}
          SPOTIFY_REFRESH_TOKEN: ${{ secrets.SPOTIFY_REFRESH_TOKEN }}
        run: |
          python archive-playlists.py \
            --archive-playlist-id "37i9dQZF1DX5Q27plkaOQ3" \
            --destination-playlist-id "6jFdULRcOzcsNJoHLU136C" \
            --log "/logs/dubstep-don-output.txt"

      - name: Commit updated log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add logs/dubstep-don-output.txt

          if git diff --cached --quiet; then
            echo "No log changes to commit."
          else
            git commit -m "Update Dubstep Don sync log"
            git push
          fi
